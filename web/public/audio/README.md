# Audio Asset Source

These audio files are copied from `murbar/jacks-or-better`:
https://github.com/murbar/jacks-or-better/tree/master/src/audio

Adopted files:

- `card-turn-alt.mp3` for new-hand and board-card deal cues
- `card-turn.mp3` for fold cues
- `button-press.mp3` for check cues
- `bet.mp3` for call / bet / raise cues
- `bet-max.mp3` for all-in cues

Additional upstream copies retained locally but not currently wired in the web app:

- `card-tap.mp3`
- `win.mp3`
- `game-over.mp3`

Upstream license:

- MIT License
- local copy: `LICENSE.murbar-jacks-or-better.txt`

At integration time, the upstream repository metadata and `LICENSE` file identified the project as MIT-licensed.

Local edits:

- none; the shipped audio files are byte-for-byte upstream copies
